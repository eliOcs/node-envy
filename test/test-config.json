{
	"environment": "development",

	"development": {
		"port": 3000,
		"database": {
			"host": "localhost",
			"name": "dev-app"
		}
	},

	"production": {
		"database": {
			"name": "prod-app"
		}
	}
}
